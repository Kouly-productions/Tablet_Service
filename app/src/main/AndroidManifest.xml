<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TabletService"
        tools:targetApi="31">
        <activity
            android:name=".AdminLoggedIn"
            android:exported="false" />
        <activity
            android:name=".LoanHistory"
            android:exported="false" />
        <activity
            android:name=".LoanTabletForm"
            android:exported="false" />
        <activity
            android:name=".UserLoggedIn"
            android:exported="false" />
        <activity
            android:name=".ui.login.userLoggedIn"
            android:exported="false"
            android:label="@string/title_activity_user_logged_in" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>